version: "3.8"

services:
  redis:
    image: redis:7-alpine
    container_name: joblink-redis
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - redis_data:/data
    restart: always
    command: redis-server --appendonly yes

volumes:
  redis_data:
